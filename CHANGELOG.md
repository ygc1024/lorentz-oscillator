# 변경 이력 (Changelog)

작업 단위(버전)별 변경사항을 기록합니다. 최신 버전이 맨 위에 옵니다.
형식: 유의적 버전 vMAJOR.MINOR.PATCH. 각 버전은 git 태그로 남깁니다.

## [v1.3.0] - 2026-07-24
### 변경
- **시료 안 빔을 Beer 법칙 프로파일로 그린다.** 이전에는 입구 높이(1−R)에서 출구 높이(T)까지
  선형으로 줄어들어, αd가 27이든 0.3이든 빔이 언제나 시료 오른쪽 끝까지 갔다.
  이제 h(z) = h₀·e^(−αz)로 그려 흡수가 강하면 빔이 시료 앞쪽에서 잦아든다
  (교재 예시의 공명에서 αd = 27.2 → 두께의 10 % 지점에서 소멸).
  투과가 남아 있을 때는 출구 높이가 T와 정확히 이어지도록 감쇠 지수를 ln(h₀/h_T)로 잡는다.
### 추가
- 흡수 봉투에 깊이 방향 그라디언트 — 흡수가 앞쪽에 몰린다는 것이 보이고,
  주황 띠가 오른쪽 끝까지 같은 진하기로 이어져 무언가 빠져나가는 것처럼 보이던 것도 사라진다.
- **흡수 깊이 1/α 표시선** — 1/α가 시료 두께 안에 들어오면 점선과 라벨로 표시.
  빔이 1/e로 줄어드는 깊이를 두께와 직접 비교할 수 있다.
### 수정
- 감쇠 지수를 계산할 때 화살표 최소 굵기(0.8 px)를 씌운 높이를 쓰는 바람에,
  투과가 0인데도 완만하게 감소해 끝까지 도달하던 문제. 이제 최소 굵기를 씌우지 않은 실제 T로 판정한다.

- 변경 파일: `index.html`
- 이전 버전: v1.2.1

## [v1.2.1] - 2026-07-24
### 수정
- **광선 도식의 흡수 표현을 바로잡음.** 반사·투과는 세로 높이가 비율(40 px = 100 %)인데
  흡수만 `46·√A`를 가로 폭으로 그리고 있어 세 값을 같은 그림에서 비교할 수 없었다.
  이제 시료 안에서 들어간 빔(높이 1−R)이 나가는 빔(높이 T)으로 얇아지고, 그 차이가
  그대로 흡수 A의 쐐기로 남는다 — 세 값 모두 같은 높이 척도. 아래로 빠지던 화살표는
  지시선과 「열로」 라벨로 대체.
- 투과가 0에 가까울 때 화살촉이 남아 있어 투과가 있는 것처럼 보이던 문제 (화살촉 크기를 T에 비례).
- `setPointerCapture` 실패 시 예외가 나던 것을 방어.

- 변경 파일: `index.html`
- 이전 버전: v1.2.0

## [v1.2.0] - 2026-07-24
### 추가
- **반사·흡수·투과 표시 방식 3종 전환** — 「누적」(R+A+T=1 보존) / 「겹쳐」(한 축 위 세 곡선 비교) /
  「따로」(각 스펙트럼 개별 패널). 「따로」에서는 값이 0.12 미만이면 축이 그에 맞게 좁혀지고,
  눈금 숫자로 실제 크기를 읽는다. 표시 방식도 URL 해시에 저장된다.
- 기준 곡선(고스트)이 세 표시 방식 모두에서 그려지도록 A·T 배열도 함께 저장.

### 수정
- y축 라벨 너비를 재서 좌측 여백을 자동 조정. 값이 아주 작아 `1.0×10⁻¹²` 같은 라벨이 나올 때
  눈금칸을 넘어 그래프를 침범하던 문제 해결.
- `[hidden]` 속성이 클래스 선택자의 `display`에 덮여 무시되던 문제 (`[hidden]{display:none!important}` 추가).
  「따로」 전환이 동작하지 않던 원인.
- **「유리의 자외선 공명」 예시 파라미터 재조정** (γ 2×10¹⁵ → 1×10¹⁴, d 1 mm → 10 µm, 보기 폭 9γ → 180γ).
  기존 값은 Lorentz 꼬리 흡수 때문에 녹색광에서 αd ≈ 176이 되어 「투명한 영역」이라는 설명과 달리
  완전 불투명했다. 새 값에서는 가시광 T ≈ 85 %, n = 1.456(적색) → 1.471(보라)로 정상분산을 보이고
  자외선에서 불투명해진다.

- 변경 파일: `index.html`, `CHANGELOG.md`
- 이전 버전: v1.1.1

## [v1.1.1] - 2026-07-24
### 수정
- `index.html`을 완전한 HTML 문서로 변경 (doctype, `<html lang="ko">`, charset, **viewport**).
  viewport 메타가 없으면 휴대폰에서 980px 폭으로 축소 렌더링되는 문제가 있었다.
  표준 모드(CSS1Compat) 렌더링도 확보.
### 추가
- `build-artifact.mjs` — Artifact 게시용 조각 파일(`artifact.html`) 생성 스크립트.
  `index.html` 하나만 손대고 배포본은 생성해서 쓴다. `artifact.html`은 git 제외.
- README에 공개 링크·비교 도구·라이선스 항목 추가.

- 변경 파일: `index.html`, `build-artifact.mjs`, `README.md`, `.gitignore`
- 이전 버전: v1.1.0

## [v1.1.0] - 2026-07-24
### 추가
- **축 고정** — 현재 x축·y축 눈금을 붙박아, 파라미터를 바꿔도 축이 다시 맞춰지지 않게 함.
  봉우리가 실제로 커지고 작아지는 것이 보인다. (자동 재조정 때문에 변화가 와닿지 않는 문제 해결)
- **기준 곡선 저장** — 현재 네 곡선과 R/A/T 경계를 점선 고스트로 남겨 지금 곡선과 겹쳐 비교.
- **두 번째 공명** — 진동자를 하나 더 켜서 (식 2.23의 j = 2) 이웃한 공명끼리 상대 비교.
  ω₀₂, γ₂, Δε₂ 슬라이더 추가, 그래프에 ω₀₁·ω₀₂ 표시선 구분.
- 대표 예시 「이웃한 두 공명」 추가 (총 7종).
- 하단 저작권·라이선스 표기 (© 2026 Young-Gwan Choi, CC BY-NC 4.0) 및 교재 출처 명기.
- 해설에 다중 공명 식(2.23)과 새 도구 사용법, 연습문제 1문항 추가 (총 7문항).

### 변경
- 표시 범위가 두 공명을 모두 포함하도록 확장, detune 축 전환 조건을 실제 표시 폭 기준으로 변경.
- URL 해시에 두 번째 공명 파라미터(tw, w2, g2, de2) 포함.

- 변경 파일: `index.html`, `CHANGELOG.md`, `README.md`
- 배포: GitHub Pages + Claude Artifact
- 이전 버전: v1.0.0
- 롤백: `git checkout v1.0.0 -- index.html`

## [v1.0.0] - 2026-07-24
### 추가
- `index.html` — Lorentz 진동자(Fox, *Optical Properties of Solids* §2.2.1, 그림 2.4) 대화형 실습 웹앱.
  단일 파일, 외부 의존성 없음.
  - ε₁·ε₂·n·κ 4패널(책 그림 2.4와 동일 배치) + 반사/흡수/투과 누적 면적 그래프 + 광선 도식
  - 파라미터: ω₀, γ, ε_st−ε_∞, ε_∞, 시료 두께 d, 보기 폭(±Nγ)
  - 대표 예시 6종: 교재 그림 2.4 / 원자 기체(Na D₂, 예제 2.1) / GaAs 격자진동 /
    NaCl 격자진동 / 유리의 자외선 공명 / 아주 센 진동자
  - 정확식(2.15·2.16) ↔ 공명 근사식(2.20·2.21) 전환, √ε₁ 및 ε₂/2n 비교선 오버레이
  - 커서 드래그 시 전 패널 동기화 + 수치 판독, 설정이 URL 해시에 저장되어 링크 공유 가능
  - 라이트/다크 테마, 반응형 레이아웃, 색각 이상 대비 검증을 통과한 팔레트
  - 해설·연습문제 6문항 포함

### 검증
- 교재 값과 대조: 그림 2.4 설정에서 ε₂ 최대 42.0 (ω₀), ε₁ 최대 31.5 / 최소 −10.5 (ω₀∓γ/2),
  n 최대 5.99, κ 최대 4.50 — 책 그림의 축 범위와 일치.
- 예제 2.1(Na D₂): κ(ω₀) = 7.92×10⁻⁵ (책 7.90×10⁻⁵), α_max = 1690 m⁻¹ (책 1.7×10³ m⁻¹),
  n(ω₀−γ/2) − 1 = 3.96×10⁻⁵ (책 3.95×10⁻⁵).

- 변경 파일: `index.html`, `CHANGELOG.md`, `README.md`, `.gitignore`
- 배포: Claude Artifact — https://claude.ai/code/artifact/8fcf32da-6678-45af-a64c-80e06ce3e3c0
- 이전 버전: 없음 (최초 릴리스)
